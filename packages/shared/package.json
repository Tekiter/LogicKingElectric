{
    "name": "@electric/shared",
    "version": "1.0.0",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "prebuild": "rimraf ./dist",
        "build": "tsc --build --clean && tsc --build"
    },
    "license": "MIT",
    "devDependencies": {
        "rimraf": "^3.0.2",
        "typescript": "^4.3.4"
    }
}
